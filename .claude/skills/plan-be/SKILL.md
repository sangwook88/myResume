---
name: plan-be
description: BE 도메인 1개의 데이터(M)와 기능(C)을 사람과 끌어내는 명세 스킬(기획 전단 3단계, BE 축). BE 도메인은 데이터+기능만 담당하고 플로우·표현은 FE가 담당한다. 소유 데이터를 데이터.md로 뽑고 큰 기능을 기능별 문서로 쪼갠다. 추상·모호한 규칙을 탐지해 구체값을 캐묻는다(구체화 엘리시테이션). 값은 짓지 않는다. 짝: plan-fe. "백엔드 도메인 만들어줘", "BE 도메인 만들어줘", "기능 쪼개줘", "plan-be" 류에 사용.
---

# plan-be — BE 도메인 명세 (기획 전단 3단계, BE 축)

BE 도메인의 *데이터(M) + 기능(C)*을 끌어낸다. 짝: [plan-fe](../plan-fe/SKILL.md)(FE 플로우+V). 전제: [decompose](../decompose/SKILL.md)로 HOME 확정. 규약: [docs/conventions.md](.claude/docs/conventions.md). 산출: `docs/be/<name>/`.

## 경계 (절대 규칙)
- BE 도메인 = **데이터(M) + 기능(C)** 만. 플로우 순서·화면·연출(V) 금지 — 그건 FE. 기능 문서는 "무슨 데이터를 읽고 어떻게 처리하나"만(언제 부르나는 FE가 정함).
- 산출: `docs/be/<name>/` 한 곳. 모든 도메인 폴더에 `일지.md` 필수(하네스).

## 표준 구조 (평평)
`README.md`(도메인폴더-README) + `데이터.md`(M 한 파일) + `기능_<name>.md`(C 기능당 1파일) + `일지.md`.

## 하드 제약
- AI는 값을 생성하지 않는다 — 수치·범위·규칙·계산식·enum 의미는 `*[입력 필요: …]*`.
- 도메인 폴더가 이미 있으면 덮어쓰기 금지(중단·되묻기). 의미 있는 변경 후 일지 기록.
- **패턴(TS/DM)은 정하지 않는다** — `README.md` 패턴 줄은 슬롯으로 두고 [arch](../arch/SKILL.md)가 채운다.

## 흐름
1. **존재 판정** — `docs/be/<name>/`이 이미 있으면 중단(기능만 추가인지 되묻기).
2. **맥락** — `CONTEXT.md`·인접 README·이 도메인을 호출하는 FE 요소 문서로 기대 기능 추림. (`일지.md`는 읽지 않는다 — write-only.)
3. **데이터·구체화 브레인스토밍** — 소유 테이블/스키마/enum을 한 번에 하나씩(식별·속성·관계). **추상·모호한 규칙("적당히 검증", "필요한 만큼")은 구체값을 캐묻는다**(수치·범위·계산식·enum 의미). 값은 사람이, AI는 [입력 필요]·질문만.
4. **기능 분리** — 큰 기능은 기능마다 1문서(`기능_<name>.md`, 한국어 파일명).
5. **골격 생성** — `.claude/templates/`의 도메인폴더-README·데이터·기능·일지를 복사·토큰 치환(기울임 안내문 유지).
6. **일지 기록** — 도메인 신설 항목 1개 + `현재 상태 요약` 갱신.

## 산출
- `데이터.md`(M) = 소유 테이블. 공유 데이터는 여기 한 번만, 기능은 링크 참조.
- `기능_<name>.md` = 기능 템플릿 5섹션. 폴더가 이미 있으면 개별 기능 파일만 신규 생성. 한국어 + 마크다운, 식별자 영문.

## 종료
생성된 BE 폴더 + 남은 슬롯 + "다음 = [distill](../distill/SKILL.md)로 가지치기·정제(슬롯 채운 뒤), 패턴은 [arch](../arch/SKILL.md)" 안내.
