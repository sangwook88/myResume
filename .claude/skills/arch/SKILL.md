---
name: arch
description: 기획 QA가 끝난 도메인들을 근거로 어떤 기술 스택을 쓰고 어떤 구조로 짤지를 트레이드오프로 결정해 전체 아키텍처 문서(docs/arch/ARCHITECTURE.md)를 쓰는 스킬(기획 전단 6단계). 기술 스택(언어·런타임·프레임워크·라이브러리·저장소)·레이어·의존·패턴 정책(TS/DM)·영속·에러·명명을 고정 목차로 그릴한다. BE 도메인 README 패턴 슬롯을 여기서 채운다. 이 문서가 티켓의 구조 근거가 된다. "아키텍처 정하자", "기술 스택 결정", "ARCHITECTURE 채워줘", "arch" 류에 사용.
---

# arch — 기술·구조 아키텍처 결정 (기획 전단 6단계)

QA로 "무엇"이 굳은 뒤 **어떤 기술로 어떤 구조로 지을지**를 트레이드오프로 결정한다. 전제: [qa](../qa/SKILL.md) 통과. 규약: [docs/conventions.md](${DDD_ROOT}/docs/conventions.md). 산출: `docs/arch/ARCHITECTURE.md` — [ticket](../ticket/SKILL.md)의 구조 근거.

## 절대 규칙
1. **트레이드오프로 결정.** 기술·구조는 갈림길로 올려 A/B(/C) 트레이드오프 + 추천 + 시나리오로 사람이 고른다(AI 단정 금지).
2. **QA된 기능에 접지.** 스택·패턴 선택을 `docs/fe/*/`·`docs/be/*/`의 실제 기능 복잡도로 정당화(막연한 선호 금지).
3. **코드로 답할 수 있으면 직접 읽는다.** 기존 코드 있으면 계승 vs 교체를 코드 근거로.
4. **한 번에 한 갈림길.** skeptic 기본 — 숨은 가정·실패 모드·기존 코드 충돌 포함.

## 시작 전 읽기
1. `docs/arch/ARCHITECTURE.md` — 현재 상태(있으면).
2. `docs/HOME.md` + `docs/fe/*/`·`docs/be/*/` — 도메인 목록·참조 그래프·QA된 기능.
3. `docs/CONTEXT.md` — 용어.
4. 코드 현황 — 빌드·디렉토리·대표 모듈(있으면).

## 고정 목차 (섹션별 그릴)
1. **기술 스택 (핵심)** — 언어·런타임 / 프레임워크(FE·BE) / 주요 라이브러리 / 저장소(DB·캐시) / 빌드·배포. 각 선택을 QA된 기능 복잡도에 접지해 트레이드오프+추천. 기존 코드 있으면 계승 vs 교체.
2. **레이어·모듈 경계** — FE 도메인 ↔ BE 도메인 2분할이 최상위. FE=플로우+V, BE=M+C. 디렉토리 매핑.
3. **의존 방향** — FE → BE 단방향(`docs/HOME.md` 참조 그래프). BE→BE도 단방향. 호출 vs 이벤트 기준.
4. **패턴 정책 (TS/DM)** — **BE 도메인**별 기본값을 표로(FE는 플로우+V라 해당 없음):
   - **트랜잭션 스크립트(TS)** = 단순 CRUD·절차적, 규칙이 얇을 때.
   - **도메인 모델(DM)** = 규칙·불변식·상태가 풍부할 때.
   - 각 BE 도메인을 둘 중 하나로 분류(애매하면 갈림길). **BE 도메인 `README.md` 패턴 슬롯을 여기서 채운다**(그 도메인 폴더 일지에 한 줄).
5. **데이터·영속** — 저장소·DTO·직렬화 규약.
6. **에러·경계** — 실패 전파·경계값 규약.
7. **명명·컨벤션** — 네임스페이스·파일·식별자(영문).
8. **미결** — 미확정 항목.

## 그릴 진행
첫 "그릴 필요" 섹션부터 **한 번에 한 갈림길**:
```
근거 — <도메인 폴더 문서/코드 경로:line>
결정할 내용: <질문>
- A) ... — 트레이드오프
- B) ... — 트레이드오프
추천: X. 시나리오: ① ... ② ...
```
- 섹션 확정 즉시 ARCHITECTURE.md 갱신. 미확정은 §8 미결로.
- **검증(§1·§4):** 대표 도메인 1개를 머릿속으로 통과시켜 "이 스택·패턴 기본값으로 티켓→코드 직행 가능한가" 확인.

## 종료
변경 요약 + 패턴 채운 BE 도메인 목록(각 폴더 `일지.md` 한 줄) + "다음 = [ticket](../ticket/SKILL.md)으로 구현 티켓" 안내. 한국어 + 마크다운, 식별자 영문.
