# be/point (v1 wave 1) 런타임 의존. 관계형 DB·그 외 의존 추가 금지(ARCHITECTURE §1, 티켓 §8).
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
PyYAML>=6.0

# be/chat (v1 wave 3) 런타임 의존.
# - langchain-anthropic: Claude claude-sonnet-5 스트리밍 + 프롬프트 캐싱(cache_control) 오케스트레이션(ARCHITECTURE §1·§4).
# - redis: 세션 이력 스토어(session_id → turns), TTL 1일 sliding(ARCHITECTURE §5).
langchain-anthropic>=0.2
langchain-core>=0.3
redis>=5.0

# be/chat (v3) RAG 서빙은 기본 OFF(CHAT_RAG_ENABLED)이며 fastembed 는 지연 임포트라,
# RAG 를 안 켜는 포크는 이 의존이 필요 없다(앱 기동·챗봇 load-all 정상). RAG 를 켤 때만
# 별도 설치: pip install -r requirements-rag.txt  (fastembed = ONNX 로컬 임베딩, 최초 1회 모델 다운로드).
