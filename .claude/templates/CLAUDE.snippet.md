## 도메인 일지 하네스 (쓰기 전용)

`일지.md`는 **쓰기만 하고 읽지 않는다** — 디버깅용 결정 로그다(쌓인 이력을 매번 읽으면 토큰만 늘고 환각을 부른다; 필요할 때 사람이 본다). 도메인 폴더(`docs/fe/<name>/`·`docs/be/<name>/`)의 문서를 의미 있게 변경(데이터 구조·규칙·스키마·enum·관계·플로우·시점 등 "이렇게 하기로 했다")한 **후에만** `일지.md`에 항목 1개를 추가하고 `현재 상태 요약`을 갱신한다. 오타·링크 정리 같은 무가치 변경은 제외.
